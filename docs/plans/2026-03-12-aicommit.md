# AI Commit Agent 实现计划

> **For Claude:** 直接开始实现，此项目结构简单不需要 worktree。

**Goal:** 实现一个 CLI 工具，分析 git staged changes，使用 AI 生成符合 conventional commits 格式的中文提交描述，支持流式输出。

**Architecture:** 使用 TypeScript + Node.js，通过简单的模块化设计实现 Git 集成、AI Provider 抽象、Prompt 构建、CLI 交互。

**Tech Stack:** TypeScript, Commander.js, inquirer, simple-git, OpenAI SDK

---

### Task 1: 初始化 TypeScript 项目

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`

**Step 1: 创建 package.json**

```json
{
  "name": "aicommit",
  "version": "1.0.0",
  "description": "AI-powered git commit message generator",
  "main": "dist/index.js",
  "bin": {
    "aicommit": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "inquirer": "^9.2.12",
    "simple-git": "^3.22.0",
    "openai": "^4.28.0",
    "ora": "^7.0.1"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
```

**Step 2: 创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**Step 3: 创建 src 目录**

```bash
mkdir -p src/git src/ai src/prompt src/config src/output src/commands
```

---

### Task 2: 实现 Git Diff 模块

**Files:**
- Create: `src/git/diff.ts`

**Step 1: 实现获取 staged diff**

```typescript
import simpleGit, { SimpleGit } from 'simple-git';

export async function getStagedDiff(): Promise<string> {
  const git: SimpleGit = simpleGit();
  const diff = await git.diff(['--cached']);
  return diff;
}

export async function hasStagedChanges(): Promise<boolean> {
  const git: SimpleGit = simpleGit();
  const status = await git.status();
  return status.staged.length > 0;
}
```

---

### Task 3: 实现 AI Provider 接口

**Files:**
- Create: `src/ai/provider.ts` (接口)
- Create: `src/ai/openai.ts` (OpenAI 适配器)
- Create: `src/ai/index.ts` (统一导出)

**Step 1: Provider 接口**

```typescript
export interface AIProvider {
  generateCommitMessage(diff: string, onChunk?: (chunk: string) => void): Promise<string>;
}

export interface ProviderConfig {
  apiKey: string;
  model?: string;
}
```

**Step 2: OpenAI 适配器**

```typescript
import OpenAI from 'openai';
import { AIProvider, ProviderConfig } from './provider';

export class OpenAIProvider implements AIProvider {
  private client: OpenAI;
  private model: string;

  constructor(config: ProviderConfig) {
    this.client = new OpenAI({ apiKey: config.apiKey });
    this.model = config.model || 'gpt-4';
  }

  async generateCommitMessage(diff: string, onChunk?: (chunk: string) => void): Promise<string> {
    const stream = await this.client.chat.completions.create({
      model: this.model,
      messages: [
        { role: 'system', content: '你是一个专业的 Git 提交助手。' },
        { role: 'user', content: diff }
      ],
      stream: true,
    });

    let fullContent = '';
    for await (const chunk of stream) {
      const content = chunk.choices[0]?.delta?.content || '';
      fullContent += content;
      onChunk?.(content);
    }
    return fullContent;
  }
}
```

---

### Task 4: 实现 Prompt 构建器

**Files:**
- Create: `src/prompt/build.ts`

```typescript
import { getStagedDiff } from '../git/diff';

export interface PromptOptions {
  language?: 'zh' | 'en';
  includeHistory?: boolean;
}

const SYSTEM_PROMPT_ZH = `你是一个专业的 Git 提交助手。

请分析以下代码变更，生成符合 conventional commits 格式的提交描述。

要求：
- type: feat/fix/docs/style/refactor/test/chore/perf/build/ci/revert 等
- 使用中文
- 简洁明了
- 格式: type(scope): subject

可选的 type 含义：
- feat: 新功能
- fix: 修复 bug
- docs: 文档变更
- style: 代码格式（不影响功能）
- refactor: 重构
- test: 测试相关
- chore: 构建过程或辅助工具变动
- perf: 性能优化
- build: 构建系统或外部依赖变更
- ci: CI 配置文件和脚本变更

请直接输出提交描述，不要有其他解释。`;

export function buildPrompt(diff: string, options: PromptOptions = {}): { system: string; user: string } {
  return {
    system: SYSTEM_PROMPT_ZH,
    user: `以下是代码变更的 diff:\n\`\`\`\n${diff}\n\`\`\``
  };
}
```

---

### Task 5: 实现流式输出模块

**Files:**
- Create: `src/output/streaming.ts`

```typescript
export function createStreamingOutput(onChunk: (text: string) => void) {
  let buffer = '';
  
  return {
    write(chunk: string) {
      buffer += chunk;
      onChunk(chunk);
    },
    getContent() {
      return buffer;
    },
    clear() {
      buffer = '';
    }
  };
}
```

---

### Task 6: 实现配置管理

**Files:**
- Create: `src/config/store.ts`

```typescript
import fs from 'fs';
import path from 'path';
import os from 'os';

export interface Config {
  provider: 'openai' | 'anthropic' | 'ollama';
  apiKey?: string;
  model?: string;
}

const CONFIG_PATH = path.join(os.homedir(), '.aicommit.json');

export function loadConfig(): Config {
  try {
    if (fs.existsSync(CONFIG_PATH)) {
      return JSON.parse(fs.readFileSync(CONFIG_PATH, 'utf-8'));
    }
  } catch (e) {
    // ignore
  }
  return { provider: 'openai' };
}

export function saveConfig(config: Config): void {
  fs.writeFileSync(CONFIG_PATH, JSON.stringify(config, null, 2));
}

export function getApiKey(): string | undefined {
  return process.env.OPENAI_API_KEY || loadConfig().apiKey;
}
```

---

### Task 7: 实现 CLI 命令

**Files:**
- Create: `src/commands/commit.ts`

```typescript
import { Command } from 'commander';
import inquirer from 'inquirer';
import ora from 'ora';
import { getStagedDiff, hasStagedChanges } from '../git/diff';
import { loadConfig } from '../config/store';
import { OpenAIProvider } from '../ai/openai';
import { buildPrompt } from '../prompt/build';

export const commitCommand = new Command('commit')
  .description('Generate commit message with AI')
  .action(async () => {
    const spinner = ora();
    
    // Check staged changes
    if (!(await hasStagedChanges())) {
      spinner.fail('No staged changes. Please run "git add" first.');
      process.exit(1);
    }

    const diff = await getStagedDiff();
    if (!diff) {
      spinner.fail('No changes to commit.');
      process.exit(1);
    }

    // Load config
    const config = loadConfig();
    const apiKey = process.env.OPENAI_API_KEY || config.apiKey;
    
    if (!apiKey) {
      spinner.fail('Please set OPENAI_API_KEY environment variable or configure apiKey.');
      process.exit(1);
    }

    // Build prompt
    const { system, user } = buildPrompt(diff);

    // Generate message
    spinner.start('Generating commit message...');
    
    let fullMessage = '';
    const provider = new OpenAIProvider({ apiKey, model: config.model });
    
    try {
      fullMessage = await provider.generateCommitMessage(user, (chunk) => {
        process.stdout.write(chunk);
      });
      console.log(); // newline after streaming
      spinner.succeed('Commit message generated!');
    } catch (error) {
      spinner.fail('Failed to generate commit message');
      console.error(error);
      process.exit(1);
    }

    // Interactive confirmation
    const { action } = await inquirer.prompt([
      {
        type: 'list',
        name: 'action',
        message: 'Commit message:',
        choices: [
          { name: 'Confirm - Use this message', value: 'confirm' },
          { name: 'Edit - Modify the message', value: 'edit' },
          { name: 'Cancel - Do not commit', value: 'cancel' }
        ]
      }
    ]);

    if (action === 'cancel') {
      console.log('Cancelled.');
      process.exit(0);
    }

    let finalMessage = fullMessage;
    
    if (action === 'edit') {
      const { editedMessage } = await inquirer.prompt([
        {
          type: 'editor',
          name: 'editedMessage',
          message: 'Edit commit message:',
          default: fullMessage
        }
      ]);
      finalMessage = editedMessage;
    }

    // Output the message for user to use
    console.log('\n--- Final Commit Message ---');
    console.log(finalMessage);
    console.log('----------------------------');
    console.log('\nUse: git commit -m "' + finalMessage.replace(/"/g, '\\"') + '"');
  });
```

---

### Task 8: 实现主入口

**Files:**
- Create: `src/index.ts`

```typescript
#!/usr/bin/env node

import { Command } from 'commander';
import { commitCommand } from './commands/commit';
import { loadConfig, saveConfig } from './config/store';

const program = new Command();

program
  .name('aicommit')
  .description('AI-powered git commit message generator')
  .version('1.0.0');

program.addCommand(commitCommand);

program.command('config')
  .description('Configure aicommit')
  .action(async () => {
    const inquirer = await import('inquirer');
    const { config: cfg } = await inquirer.default.prompt([
      {
        type: 'list',
        name: 'provider',
        message: 'Select AI provider:',
        choices: ['openai', 'anthropic', 'ollama'],
        default: 'openai'
      }
    ]);
    
    const config = loadConfig();
    config.provider = cfg;
    saveConfig(config);
    console.log('Configuration saved.');
  });

program.parse(process.argv);
```

**Step 2: 创建 package.json bin 字段指向**

确保 package.json 中添加：
```json
"bin": {
  "aicommit": "dist/index.js"
}
```

---

### Task 9: 测试构建

**Step 1: 安装依赖**

```bash
npm install
```

**Step 2: 编译 TypeScript**

```bash
npm run build
```

**Step 3: 测试运行**

```bash
# 在一个有 git 仓库的目录测试
cd /some/git/repo
node /path/to/aicommit/dist/index.js commit
```

---

### 预计产出

实现完成后，你将有一个可用的 CLI 工具，可以：
1. 分析 staged changes
2. 调用 OpenAI API 生成中文 commit message
3. 流式输出结果
4. 交互确认或编辑
5. 提示用户执行 git commit
